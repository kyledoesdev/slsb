export default {
    data() {
        return {
            'currentRoute': $('#page_name').val(),
            'authId': $('#auth_id').val(),
            'authUserName': $('#auth_user_username').val(),
        }
    }
}